#include <stdio.h>
#include <math.h>

//double calcHypotenus(double a, double b);


int main(){
    double a,b;
    printf("Enter A value: ");
    scanf("%lf", &a);
    printf("Enter B value: ");
    scanf("%lf", &b);
   // printf("The hypotenus is %.3lf",calcHypotenus(a,b));
    return 0;
}
void calcHypotenus(double a, double b){
    //return sqrt(pow(a,2)+pow(b,2));
}