#include <stdio.h>

int main() {
    double price[] = {2.0,3.0,4.5,5.6,2.3};
    double numbers[][1]={};

    //instead of like price length u gotta do this
    for(int i = 0; i< sizeof(price)/sizeof(price[0]);i++){

    }
    return 0;
}